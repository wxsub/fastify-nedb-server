{
  "name": "fastify-nedb-server",
  "version": "1.0.0",
  "description": "This is a backend service project template using Fastify, a popular framework in the Node.js community, combined with the local database tool NeDB. This template allows you to quickly build server-side projects based on Fastify.",
  "type": "module",
  "main": "app.js",
  "directories": {
    "plugins": "plugins",
    "routes": "routes"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --watch-path routes --watch-path plugins server.js"
  },
  "keywords": ["nodejs", "http-server", "nedb", "fastify"],
  "author": "wxsub system lnc.",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^6.0.0",
    "@fastify/sensible": "^6.0.0",
    "fastify": "^5.0.0",
    "fastify-cli": "^7.4.0",
    "fastify-plugin": "^5.0.0",
    "moment": "^2.30.1",
    "nedb": "^1.8.0"
  },
  "imports": {
    "#util/*": "./utils/*"
  }
}
